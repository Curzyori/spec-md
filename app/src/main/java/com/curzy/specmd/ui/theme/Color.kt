package com.curzy.specmd.ui.theme

import androidx.compose.ui.graphics.Color

// Brand & Accent
val Primary = Color(0xFF3B82F6)        // Electric blue
val PrimaryHover = Color(0xFF2563EB)     // Darker blue for press
val PrimarySoft = Color(0xFFDBEAFE)     // Light blue tint

// Background & Surface
val BgBase = Color(0xFF0D0D0D)        // Near-black base
val BgElevated = Color(0xFF141414)     // Card surfaces
val BgOverlay = Color(0xFF1A1A1A)      // Bottom sheets, modals
val BgInput = Color(0xFF1F1F1F)       // Input fields

// Borders & Dividers
val Border = Color(0xFF262626)          // Subtle borders
val BorderHover = Color(0xFF333333)      // Hover state borders

// Text
val TextPrimary = Color(0xFFFAFAFA)       // Primary text - near white
val TextSecondary = Color(0xFFA1A1AA)   // Secondary/muted text
val TextTertiary = Color(0xFF71717A)   // Tertiary/disabled
val TextOnAccent = Color(0xFFFFFFFF)    // Text on blue accent

// Semantic
val Success = Color(0xFF22C55E)        // Green - copy success
val Warning = Color(0xFFF59E0B)        // Amber - caution
val Error = Color(0xFFEF4444)         // Red - errors
val Info = Color(0xFF3B82F6)          // Blue - informational
